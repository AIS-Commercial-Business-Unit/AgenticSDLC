{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ais.com/agentic-sdlc/framework/schemas/initialization-state.schema.json",
  "title": "AIS Agentic Engineering Framework — Repository Initialization State",
  "description": "JSON Schema 2020-12 for tracking the overall framework initialization state of a target repository. Written to docs/assessment/initialization-state.yaml during the brownfield initialization workflow.",
  "type": "object",
  "required": ["$schema", "version", "initialized_at", "last_updated", "repo", "framework_version", "phase", "completed_steps", "pending_steps"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema URI for this document."
    },
    "version": {
      "type": "string",
      "description": "Initialization state schema version (semver).",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "initialized_at": {
      "type": "string",
      "description": "ISO-8601 timestamp when framework initialization was first started on this repository.",
      "format": "date-time"
    },
    "last_updated": {
      "type": "string",
      "description": "ISO-8601 timestamp of the most recent state change.",
      "format": "date-time"
    },
    "repo": {
      "type": "string",
      "description": "Repository identifier — path on disk or GitHub owner/repo slug."
    },
    "framework_version": {
      "type": "string",
      "description": "Version of the AIS Agentic Engineering Framework used for this initialization.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "phase": {
      "type": "string",
      "description": "Current initialization lifecycle phase. Advances forward only — never revert phase without explicit override.",
      "enum": ["not_started", "assessing", "initializing", "configured", "active"]
    },
    "assessment_path": {
      "type": ["string", "null"],
      "description": "Relative path to the readiness-assessment.yaml file produced by the assessment step. Null until the assessment is complete."
    },
    "config_path": {
      "type": ["string", "null"],
      "description": "Relative path to the aispec.config.yaml file written during initialization. Null until the initialization step is complete."
    },
    "completed_steps": {
      "type": "array",
      "description": "Ordered list of initialization steps that have been completed and verified. Valid step names: assessment, questionnaire, config, governance-registry, agent-catalog, work-management, mcp-config, branch-protection, pr-template, codeowners, documentation.",
      "items": {
        "type": "string",
        "enum": [
          "assessment",
          "questionnaire",
          "config",
          "governance-registry",
          "agent-catalog",
          "work-management",
          "mcp-config",
          "branch-protection",
          "pr-template",
          "codeowners",
          "documentation"
        ]
      }
    },
    "pending_steps": {
      "type": "array",
      "description": "Ordered list of initialization steps that have not yet been completed. This list shrinks as steps move to completed_steps.",
      "items": {
        "type": "string",
        "enum": [
          "assessment",
          "questionnaire",
          "config",
          "governance-registry",
          "agent-catalog",
          "work-management",
          "mcp-config",
          "branch-protection",
          "pr-template",
          "codeowners",
          "documentation"
        ]
      }
    },
    "work_management_provider": {
      "type": ["string", "null"],
      "description": "The work management provider configured for this repository. Null until the work-management step is complete.",
      "enum": ["github-issues", "jira", "azure-devops", "mock", null]
    },
    "notes": {
      "type": "array",
      "description": "Append-only log of timestamped notes recorded during initialization. Never edit or remove existing notes.",
      "items": {
        "type": "object",
        "required": ["timestamp", "note"],
        "additionalProperties": false,
        "properties": {
          "timestamp": {
            "type": "string",
            "description": "ISO-8601 timestamp when this note was recorded.",
            "format": "date-time"
          },
          "note": {
            "type": "string",
            "description": "Free-text note, decision, or observation recorded during initialization."
          }
        }
      }
    }
  }
}
